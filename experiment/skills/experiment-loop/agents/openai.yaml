interface:
  display_name: "Experiment Loop"
  short_description: "Plan, run, and materialize traceable experiments"
  default_prompt: "Use $experiment-loop to run this experiment with traceable Protocol and Fact outputs."
