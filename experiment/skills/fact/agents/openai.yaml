interface:
  display_name: "Fact"
  short_description: "Materialize a Protocol run as structured evidence"
  default_prompt: "Use $fact to materialize this experiment run as a validated internal Fact."
