interface:
  display_name: "Protocol"
  short_description: "Define and validate reproducible artifact lineage"
  default_prompt: "Use $protocol to define the reproducible data-lineage contract for this artifact."
