interface:
  display_name: "Workspace Skill"
  short_description: "Sync untracked local files across workspaces"
  default_prompt: "Use $workspace-skill to initialize and apply local file sharing rules across project workspaces."
